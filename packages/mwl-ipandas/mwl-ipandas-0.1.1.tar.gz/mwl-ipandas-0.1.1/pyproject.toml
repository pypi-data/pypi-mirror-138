[tool.poetry]
name = "mwl-ipandas"
version = "0.1.1"
description = ""
authors = ["David Lassner <lassner@tu-berlin.de>"]
readme = "README.md"

[tool.poetry.scripts]
ipandas = "mwl_ipandas.main:app"

[tool.poetry.dependencies]
python = "^3.8"
typer = {extras = ["all"], version = "^0.4.0"}
ipdb = "^0.13.9"
pandas = "^1.4.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
