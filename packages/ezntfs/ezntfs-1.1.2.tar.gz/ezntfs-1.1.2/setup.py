#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['ezntfs']

package_data = \
{'': ['*']}

install_requires = \
['pyobjc']

entry_points = \
{'console_scripts': ['ezntfs = ezntfs.cli:main'],
 'gui_scripts': ['ezntfs-app = ezntfs.app:main']}

setup(name='ezntfs',
      version='1.1.2',
      description='An easy-to-use wrapper for NTFS-3G on macOS.',
      author='Matt',
      author_email='lezgomatt@gmail.com',
      url='https://github.com/lezgomatt/ezntfs',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
